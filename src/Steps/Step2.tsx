type props = {
	activeStep: number;
	setActiveStep: React.Dispatch<React.SetStateAction<number>>;
};

const Step2 = ({ activeStep, setActiveStep }: props) => {
	return <div>Step1</div>;
};

export default Step2;
